{
  "name": "acme-finance-suite",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "python -m pytest backend",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
